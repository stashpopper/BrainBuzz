# Production environment variables
# Replace with your actual MongoDB connection string
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/brainbuzz?retryWrites=true&w=majority

# JWT secret for authentication tokens - change to a secure random string
JWT_SECRET=hritam

# Set NODE_ENV to production
NODE_ENV=production